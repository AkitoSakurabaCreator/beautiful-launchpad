# Swift Package Manager / build artifacts
.build/
*.app/

# Xcode (in case the project is opened there later)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# Optional embedded icon (local asset)
AppIcon.icns

# Release / Sparkle build artifacts (produced by build-app.sh and CI)
/Launchpad.zip
/BeautifulLaunchpad.zip
/updates/
/sparkle-tools/
/sparkle.tar.xz

# Local-only icon source variant (not consumed by build-app.sh)
/icon-original.png

# macOS
.DS_Store
